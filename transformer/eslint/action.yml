name: 'eslint transformer of common lint reporter'
description: 'transformer of eslint report file to common lint report'
author: 'MeilCli'
branding:
  icon: layers
  color: orange
inputs:
  report_files:
    description: 'report file glob pattern'
    required: true
  report_files_follow_symbolic_links:
    description: 'report file glob pattern option'
runs:
  using: 'node12'
  main: '../../dist/transformer/eslint/index.js'
